MACRO(OPENMEEG_UNIT_TEST TEST_NAME)
    NEW_EXECUTABLE(${TEST_NAME} ${ARGN})
    IF (WIN32)
        SET(TEST_PATH ${EXECUTABLE_OUTPUT_PATH})
    ELSE()
        SET(TEST_PATH ${CMAKE_CURRENT_BINARY_DIR})
    ENDIF()
    ADD_TEST(${TEST_NAME} ${TEST_PATH}/${TEST_NAME})
ENDMACRO()
