--- a/niftilib/CMakeLists.txt
+++ b/niftilib/CMakeLists.txt
@@ -15,7 +15,7 @@ IF (BUILD_SHARED_LIBS)
 ENDIF (BUILD_SHARED_LIBS)

 IF(UNIX)
-  TARGET_LINK_LIBRARIES(${NIFTI_NIFTILIB_NAME} -lm)
+  TARGET_LINK_LIBRARIES(${NIFTI_NIFTILIB_NAME} m)
 ENDIF(UNIX)

 IF(NOT NIFTI_INSTALL_NO_LIBRARIES)
